<header class="sub-header">

    <div class="nav sub-nav">
        <input type="checkbox" id="nav-check">
        <div class="nav-header">
            <div class="nav-logo">
                <img src="/img/logo.png" alt="">
            </div>
            <div class="nav-title">
                <h2>
                    
                    <% if(main){ %>
                        <%- main.titel %>
                      <% } else{ %>  
                        Firmenname
                     <% } %>

                </h2>
            </div>
        </div>
        <div class="nav-btn">
          <label for="nav-check">
            <span></span>
            <span></span>
            <span></span>
          </label>
        </div>
        
        <div class="nav-links">
            <a href="/">Startseite</a>
        <% if(unterseiten){ %>
        <% for(var i = 0; i <= unterseiten.length - 1; i++) { %>
            <a href="<%= unterseiten[i].titel %>"><%= unterseiten[i].titel %></a>
        <% } %>
        <% } %>
        </div>
      </div>
</header>